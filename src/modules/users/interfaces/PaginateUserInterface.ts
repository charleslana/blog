import User from "../typeorm/entities/User";

export default interface PaginateUserInterface {
    from: number;
    to: number;
    per_page: number;
    total: number;
    current_page: number;
    prev_page: number | null;
    next_page: number | null;
    data: User[];
}